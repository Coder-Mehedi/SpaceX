import React from 'react';
import {Text} from 'react-native';
import {Colors} from 'utils/colors';

const Cores = () => {
  return <Text style={{color: Colors.primaryText}}>Cores</Text>;
};

export default Cores;
